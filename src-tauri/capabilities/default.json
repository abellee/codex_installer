{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Codex setup window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
